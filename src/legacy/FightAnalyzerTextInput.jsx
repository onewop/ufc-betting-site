/**
 * ARCHIVED — Fight Analyzer Text Input (removed from FightAnalyzer UI for cleaner layout)
 * Removed: April 15, 2026
 *
 * HOW TO RESTORE:
 * 1. In FightAnalyzer.jsx, place the JSX block below between the dropdown </div>
 *    and the Analyze <button>.
 * 2. Remove the auto-default question line from the fight-select onClick handler:
 *      setQuestion("Who wins? Overall fight prediction.");
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * JSX — paste between the custom dropdown div and the Analyze button:
 * ─────────────────────────────────────────────────────────────────────────────
 */

/*
        <input
          type="text"
          value={question}
          onChange={(e) => setQuestion(e.target.value)}
          placeholder="Ask about striking, takedowns, or records..."
          className="border border-yellow-700/40 bg-stone-900 text-stone-100 p-2 rounded-lg w-full mb-4 min-h-[44px] text-sm"
        />
*/
